# Steganography
Steganography means hiding a secret message inside another normal file so that no one knows a message is hidden. For example, we can hide text inside an image by slightly changing the smallest bits of the image pixels. The image looks the same to the human eye, but the hidden message can be extracted later using a decoding program.
